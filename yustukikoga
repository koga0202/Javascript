# include<studio.h>

int main() {
  int i;
  for(i=1; i<=20; i++){
    char tmp[9] ={};
    if (i % 3 == 0) sprintf(tmp,"Fizz");
    if (i % 5 == 0) sprintf(tmp,"%sBuzz",tmp);
    if (!*tmp) sprintf(tmp, "%d", i);
    puts(tmp);
  }
  return
 }
